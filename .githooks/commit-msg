#!/bin/sh
# Generated by git-workflow-init v4.1.0
MSG=$(cat "$1")
SUBJECT=$(printf '%s\n' "$MSG" | head -1)
SUBJECT=$(printf '%s\n' "$SUBJECT" | sed '/^#/d' | sed 's/^[[:space:]]*//')
[ -z "$SUBJECT" ] && exit 0

# --- BEGIN MANAGED: commit-msg/format (hash:a7a6f9f1) ---
TYPES="feat|fix|chore|docs|refactor|test|style|perf"
if ! printf '%s\n' "$SUBJECT" | grep -qE "^($TYPES)(\(.+\))?: .+"; then
    echo "❌ 提交信息格式不符合 Conventional Commits 规范。"
    echo "   期望格式：<type>[(<scope>)]: <subject>"
    echo "   有效 type：feat, fix, chore, docs, refactor, test, style, perf"
    exit 1
fi
# --- END MANAGED: commit-msg/format ---

# --- BEGIN MANAGED: commit-msg/length (hash:b80ab7ee) ---
LEN=$(printf '%s' "$SUBJECT" | wc -c | tr -d ' ')
if [ "$LEN" -gt 80 ]; then
    echo "❌ 提交信息首行超过 80 个字符（当前 ${LEN} 个字符）"
    exit 1
fi
# --- END MANAGED: commit-msg/length ---

exit 0
